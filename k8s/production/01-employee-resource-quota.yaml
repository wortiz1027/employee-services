# +--------------------------------------------------
# | CREATE RESOURCE QUOTA FOR PRODUCTION ENVIRONMENT
# | AUTHOR: WORTIZ
# | DATE: 09-01-2022
# +--------------------------------------------------

apiVersion: v1
kind: ResourceQuota
metadata:
  name: rq-production
  namespace: ns-production
  labels:
    owner: taa
    env: prd
    type: configuration
    app: api-employees
spec:
  hard:
    requests.cpu: "1"
    requests.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi