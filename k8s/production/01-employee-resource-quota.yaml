# +--------------------------------------------------
# | CREATE RESOURCE QUOTA FOR PRODUCTION ENVIRONMENT
# | AUTHOR: WORTIZ
# | DATE: 09-01-2022
# +--------------------------------------------------

apiVersion: v1
kind: ResourceQuota
metadata:
  name: rq-production
  namespace: ns-production
  labels:
    owner: taa
    env: dev
    type: configuration
    app: api-employees
spec:
  hard:
    request.cpu: "1"
    request.memory: 1Gi
    limits.cpu: "2"
    limits.memory: 2Gi