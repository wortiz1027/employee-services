# +--------------------------------------------------
# | CREATE RESOURCE QUOTA FOR DEVELOP ENVIRONMENT
# | AUTHOR: WORTIZ
# | DATE: 09-01-2022
# +--------------------------------------------------

apiVersion: v1
kind: ResourceQuota
metadata:
  name: rq-development
  namespace: ns-development
  labels:
    owner: taa
    env: dev
    type: configuration
    app: api-employees
spec:
  hard:
    requests.cpu: "500m"
    requests.memory: "500m"
    limits.cpu: "1"
    limits.memory: "990mi"