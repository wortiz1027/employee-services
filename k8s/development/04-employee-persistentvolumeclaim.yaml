apiVersion: v1
kind: PersistentVolumeClaim
metada:
  name: pvc-development
  namespace: ns-development
  labels:
    app: api-employees
    owner: taa
    env: prd
    type: configuration
spec:
  storageClassName: nfs
  accessModes:
    - ReadWriteMany
  resources:
    request:
      storage: 1Gi